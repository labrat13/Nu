Надо разработать стандарт на движки.

Раз уж я сделал шаблон для движков, то теперь хорошо бы их все и унифицировать и обеспечить единый интерфейс.

Оформить в коде как Интерфейс, и прописать в нем стандартные функции интерфейса движка.
Или как базовый класс движка? Тогда в нем можно реализовать некоторые сервисные функции.
- в нем должно быть свойство КлассДвижка. Чтобы различать как-то движки по применению.
- И еще интерфейс УАМХ для Сущностей надо доработать и тоже здесь применить. Если возможно.
- Проблема в том, что надо где-то хранить этот базовый класс, в одном месте. 
  В некоторой dll хранить такой базовый класс и брать из него классы в каждый Движок.
  А общий пользователь всех этих движков будет тогда этот класс заимствовать для работы с Движками.
- Интерфейсы в этом смысле лучше, чем классы? 
-- я конечно могу интерфейс написать одним файлом и потом в каждой сборке его вставлять как кусок кода.
   Но будет ли это одним и тем же интерфейсом, с точки зрения кода пользовательского приложения?
   Надо этот вопрос выяснить.
-- мне нужен один интерфейс на все сборки движков. Чтобы я мог применить его к сборке движка и работать с ним.
   А для этого надо сборку загружать динамически, в отдельный домен приложения, а не линковать статически, как я привык.
   Но вот и общий интерфейс должен работать в таком случае.
-- Есть COM - компоненты, они как раз такой вот способ и интерфейсы у них для этого. 
   Но этот вопрос надо изучить.


Решено создать движки и прочее в новом стандарте УАМХ-2. 
Сборка UAMX.dll будет содержать классы для Движка и сам базовый класс Движка.  
Создан проект, разрабатывается концепция УАМХ-2 и сборки.
Там работы на месяц, если работать.
   