В список действий с объектами (где у меня этот список для разоработки?) добавить:
- собирать в коллекции (наборы, списки)
- сохранять один объект в хранилище (файл, БД, итп)
- передавать объект по сети
- принимать объект по сети
- копировать/переносить один объект на другое хранилище.
- ... тодо: добавить идеи здесь

Коллекции объектов:
- сохранять коллекцию в хранилище (файл, БД, итп)
- копировать/переносить коллекцию в другое хранилище.
+ добавить операции с Коллекциями объектов как самостоятельным объектом.
- ... тодо: добавить идеи здесь

Версии объектов:
- сравнивать версии объекта из разных наборов (так как у них разная история и жизненный цикл)
- изменять версии объектов
- заменять старые объекты на новые (мержить коллекции)
- выявлять конфликты версий и автоматически разрешать их
- выявлять изменения двух объектов независимо от версий (сравнивать, см выше, но производить список отличий)
+ добавить операции с Версиями объектов как самостоятельным объектом.
- ... тодо: добавить идеи здесь

Хранилища 
- это место хранения объектов. Например, на компе, на сервере в интернете, на смартфоне, в локальной сети.
  В виде файлов или БД.
- хранилища должны иметь версию набора данных, чтобы их можно быстро сравнивать.
- хранилища должны поддерживать транзакции, чтобы исправлять ошибки на месте.
- хранилища должны иметь версию устройства (версию движка)
- хранилища должны иметь свойство - индикатор состояния, чтобы быстро определять состояние в движке.
- хранилища должны иметь процедуру проверки целостности для случая внешних повреждений.
- хранилища должны иметь процедуру восстановления после повреждений.
- ... тодо: добавить идеи здесь


Вывод: в УАМХ надо добавить объекты ВерсияЭлемента, КоллекцияЭлементов, и операции с ними.
- возможно, Элемент это базовый клас для ВерсионированныйЭлемент. 
  - Поскольку версии элементов не всегда нужны, а процессы для них отличаются от элементов без версионирования.
- возможно, КоллекцияЭлементов это базовый класс для КоллекцияВерсионированныхЭлементов. 
